const initialState = {
  navigation:[],
  currentStory:0,
  stories:[
  ]
};

export default function(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
